<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.script.local.mapper.SyncDbMapper">
    <insert id="insertData" parameterType="com.example.script.common.entity.SyncDb">
        INSERT INTO sync_db (id,version_id, schema_name, create_time,update_time)
        VALUES (#{id},#{versionId}, #{schemaName}, #{createTime},#{createTime})
    </insert>
</mapper>